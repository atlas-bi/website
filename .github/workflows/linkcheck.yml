name: 🔗 Linkcheck
on: [push]
jobs:
  linkcheck:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
    - name: 💾 Checkout
      uses: actions/checkout@v6

    - name: ⎔ Setup node
      uses: actions/setup-node@v5
      with:
        node-version: 22

    - name: 📥 Download deps
      uses: bahmutov/npm-install@1a205a079a5a87cbf2753b117699494c5ea05169
      with:
        useLockFile: false

    - name: 🏗️ Build
      run: npm run build:ci

    - name: 🔗 Linkcheck
      uses: untitaker/hyperlink@d72fd9923d8a82394e29ac630a92a4c3617b714f
      with:
        args: _site/