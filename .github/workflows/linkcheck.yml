name: 🔗 Linkcheck
on: [push]
jobs:
  linkcheck:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
    - name: 💾 Checkout
      uses: actions/checkout@v5

    - name: ⎔ Setup node
      uses: actions/setup-node@v5
      with:
        node-version: 18

    - name: 📥 Download deps
      uses: bahmutov/npm-install@3e063b974f0d209807684aa23e534b3dde517fd9
      with:
        useLockFile: false

    - name: 🏗️ Build
      run: npm run build:ci

    - name: 🔗 Linkcheck
      uses: untitaker/hyperlink@e66bb17cc9ae341677431edec3b893a0aa6ac747
      with:
        args: _site/